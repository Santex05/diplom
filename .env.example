# URL бэкенда. Для разработки ОСТАВЬТЕ ПУСТЫМ — запросы пойдут через proxy Vite (vite.config.js).
# Не указывайте http://localhost:3001 — иначе баннеры и постеры могут не отображаться.
# VITE_API_URL=
